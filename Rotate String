class Solution {
    public boolean rotateString(String A, String B) {
        if (A.length() != B.length())
			return false;
        String AB=A+A;
        if(AB.contains(B))
            return true;
        else
            return false;
    }
}
